# Diagnostics reports

This folder is generated/used by `scripts/collect_diagnostics.py`.

The generated `diagnostics_*.txt` files contain:
- `scripts/doctor.py` output
- `scripts/i18n_audit.py` output

Notes:
- Absolute paths are redacted as `<ABS_PATH>`.
- Reports are intended to be shared in chat without leaking local machine paths.
